"use strict";

define([
		"app",
	],
	function ( app ) {
		app.config(function( $routeProvider ) {

			// $routeProvider.when( "/view1", routeConfig.config( "../template/view1.html", "controllers/first", {
			// 	directives: [ "directives/version" ],
			// 	services: [],
			// 	filters: [ "filters/reverse" ]
			// })).otherwise({
			// 	redirectTo:"/"
			// });
		});
	}
);
